// MVP business is FR-only (Pays de la Loire) — Europe/Paris is the only
// time zone that matters for date formatting + booking calendar.
// Internationalization V2+ may make this per-locale or per-user.
export const DEFAULT_TIME_ZONE = 'Europe/Paris';
