{
  "name": "@tukio/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Backend JWT validation + Keycloak adapter (server-side auth).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" --max-warnings=0",
    "typecheck": "tsc --noEmit"
  }
}
