# CLAUDE.md

Claude Code auto-loads this file at the start of every session. The `@`
syntax below imports the project's agent conventions (entry point + ACS
manifest + code style) so they are always in context — no manual `Read`
needed.

@AGENTS.md

@.agents/acs.yaml

@.agents/context/code-style.md
