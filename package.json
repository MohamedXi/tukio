{
  "name": "tukio",
  "version": "0.0.0",
  "private": true,
  "description": "Tukio — Event-services marketplace (B2B2C). Pays de la Loire pilot.",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22.16.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "turbo run dev --concurrency=20",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky || true",
    "docker:up": "docker compose -f infra/docker-compose/docker-compose.dev.yml up -d",
    "docker:up:wait": "docker compose -f infra/docker-compose/docker-compose.dev.yml up -d --wait",
    "docker:down": "docker compose -f infra/docker-compose/docker-compose.dev.yml down",
    "docker:down:volumes": "docker compose -f infra/docker-compose/docker-compose.dev.yml down -v",
    "docker:logs": "docker compose -f infra/docker-compose/docker-compose.dev.yml logs -f",
    "docker:reset": "pnpm docker:down:volumes && pnpm docker:up:wait && pnpm docker:bootstrap",
    "docker:bootstrap": "bash infra/scripts/bootstrap-databases.sh && pnpm keycloak:themes:build && bash infra/scripts/bootstrap-keycloak-realm.sh --env=local --skip-phasetwo-webhook && bash infra/scripts/smoke-test-keycloak-realm.sh --env=local --skip-phasetwo-tests && pnpm seed:categories",
    "keycloak:bootstrap": "bash infra/scripts/bootstrap-keycloak-realm.sh --env=local",
    "keycloak:export": "bash infra/scripts/bootstrap-keycloak-realm.sh --env=local --export-only",
    "keycloak:smoke": "bash infra/scripts/smoke-test-keycloak-realm.sh --env=local",
    "keycloak:themes:build": "bash infra/scripts/build-keycloak-themes.sh",
    "docker:test:up": "docker compose -f infra/docker-compose/docker-compose.test.yml up -d --wait",
    "docker:test:up:slow": "docker compose -f infra/docker-compose/docker-compose.test.yml --profile slow-services up -d --wait",
    "docker:test:down": "docker compose -f infra/docker-compose/docker-compose.test.yml --profile slow-services down -v",
    "seed:categories": "tsx infra/scripts/seed-categories.ts",
    "chaos:test": "bash infra/scripts/run-chaos-tests.sh"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@tailwindcss/oxide",
      "sharp",
      "unrs-resolver",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "@types/node": "^22.0.0",
    "@types/pg": "^8.20.0",
    "eslint": "^9.0.0",
    "eslint-plugin-boundaries": "^5.0.0",
    "husky": "^9.1.0",
    "lint-staged": "^16.1.0",
    "pg": "^8.20.0",
    "prettier": "^3.5.0",
    "tsx": "^4.22.1",
    "turbo": "^2.9.14",
    "typescript": "^5.6.0"
  }
}
